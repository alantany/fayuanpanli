# 法院判例知识库 - 云端最小化版本依赖
# 不包含embedding支持，适用于编译困难的环境

# 核心Web框架
flask==2.3.3

# 向量数据库（仅查询功能）
chromadb==0.4.15

# 文本处理（集合名称生成需要）
pypinyin==0.49.0

# HTTP请求（LLM分析需要）
requests==2.31.0

# 环境变量加载
python-dotenv==1.0.0

# 生产环境WSGI服务器
gunicorn==21.2.0 