# 法院判例知识库 - 云端查询专用版依赖
# 适用于1C2G低资源环境

# 核心Web框架
flask==2.3.3

# 向量数据库（仅查询功能）
chromadb==0.4.15

# 文本处理（集合名称生成需要）
pypinyin==0.49.0

# HTTP请求（LLM分析需要）
requests==2.31.0

# 环境变量加载
python-dotenv==1.0.0

# 生产环境WSGI服务器
gunicorn==21.2.0 